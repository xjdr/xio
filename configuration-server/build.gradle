plugins {
  id 'java'
  id 'application'
}

description = 'trailhead'
group = project_group

mainClassName = 'com.xjeffrose.xio.server.Main'

dependencies {
  implementation project(':xio-core')
  implementation group: 'org.apache.curator', name: 'curator-test', version: curator_version
}
