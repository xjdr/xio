machine:
  java:
    version: oraclejdk8
  pre:
    - sudo apt-get update
    - sudo apt-get install openssl libssl-dev
dependencies:
  cache_directories:
    - ~/.m2
test:
  override:
    - mvn package
  post:
    - cp target/xio*jar $CIRCLE_ARTIFACTS
